# VibeReading 双陪伴模式改造计划

## 0. 给执行 Agent 的任务说明

你将把当前 VibeReading Skill 从“每本书生成一种复杂互动空间”，改造成两个稳定、克制、可复用的阅读陪伴播放器：

- `window`：以章节或阅读阶段对应的窗景、天气、环境声与浮现文字陪伴阅读。
- `vinyl`：以章节或阅读阶段对应的唱片、纯音乐、封套文字与氛围变化陪伴阅读。

两个模式共享一段简短的互动式入静引导和一套预制陪伴控件。

本任务必须持续推进到实现、测试、视觉验收和文档收束全部完成。不要只修改 Prompt 或写设计说明。

### 当前工作分支

```text
redesign/two-companion-modes
```

开始工作前确认自己位于该分支。不得合并到 `master`，不得改写已有 Git 历史。

### 无人值守执行原则

- 不向用户等待式提问。遇到非关键歧义时，按本计划中的默认决策继续。
- 每完成一个里程碑，运行该阶段测试并创建一个独立 Git commit。
- 不调用图片生成 Skill，不调用音乐生成 API，不读取或索要 API key。
- 开发测试所需图片和 BGM 从本文指定的历史 `output/` 中复制到新的测试输出目录。
- 历史 `output/` 只读，不得修改或删除。
- 新测试输出仍写入 `output/`，该目录已被 Git 忽略。
- 文档尽量使用中文；代码标识、JSON 字段、库名和必要技术术语保留英文。
- 不为了“显得有创意”重新增加谜题、任务、关系拓扑或复杂探索机制。
- 如果某一步失败，先尝试修复；只有同一外部阻塞连续出现三次且无法绕过时，才记录阻塞并继续完成其他部分。

---

## 1. 改造目标

### 1.1 产品定义

VibeReading 不再负责为每本书发明一个互动装置。

它负责根据书籍生成一组可以持续停留的阅读氛围：

```text
短暂入静引导
→ 建立读前坐标
→ 进入 Window 或 Vinyl 陪伴空间
→ 按章节或阅读阶段切换画面 / 唱片
→ 在低干扰状态中持续阅读
```

页面的首要价值是陪伴，而不是展示交互创意。

本轮改造的主要创造力不再投入复杂交互，而应投入**逐阶段的个性化视听表达**。
每个阅读阶段都必须像由同一位导演完成的一场戏：图片、BGM、环境声、天气、文字、动画与局部 UI 共同表达当前阶段的气质。

稳定运行时只是容器。最终页面不能因为复用了 Window 或 Vinyl，就失去这本书独有的语言、材质、节奏和情绪变化。

### 1.2 成功标准

一个第一次接触该书的读者，应当能够：

1. 在 20 秒内理解这本书大致是什么、阅读时可以留意什么；
2. 在入静引导中自然认识页面最必要的功能；
3. 无需阅读说明书即可开始阅读陪伴；
4. 手动切换章节或阅读阶段；
5. 每个阶段拥有专属图片与专属纯音乐 BGM，并感受到天气、环境声、文字、动画和局部 UI 随阶段共同变化；
6. 能明显感受到不同阶段属于同一本书，但具有不同的情绪位置；
7. 在 16:9 桌面屏幕中看到完整主要内容，不发生上下拥挤或关键内容裁切；
8. 在移动端完成相同核心操作；
9. 直接双击 `index.html` 通过 `file://` 使用核心能力。

### 1.3 非目标

本轮明确不做：

- 不保留八种活跃模板；
- 不生成谜题、任务、解谜关系或复杂探索；
- 不要求 `entry / exploration / companion` 三段协议状态；
- 不要求每本书重新设计导航与交互面板；
- 不要求 Three.js；唱机优先使用稳定的 DOM/CSS 实现；
- 本次无人值守开发不调用生图或音乐 API，但必须完整设计并实现后续生成任务所需的图片与 BGM 指导流程；
- 不强制按真实章节拆分；
- 不把页面做成书籍百科、剧情摘要或电子书阅读器。

---

## 2. 核心体验设计

## 2.1 共享入静引导

Window 与 Vinyl 使用同一个预制引导组件，但通过主题变量呈现不同气质。

引导应类似冥想或瑜伽课程开头：短、安静、文字有感染力，不做冗长教程。

### 默认流程

总时长建议 18–30 秒，分为四步：

```text
settle      安静下来
orient      建立读前坐标
introduce   认识必要操作
begin       正式进入阅读
```

示例节奏：

1. `settle`
   - 天气与画面先出现，UI 保持隐藏。
   - 显示一句呼吸提示，例如“先不用急着翻页。慢慢吸气。”
   - 文字随呼吸节奏淡入淡出，不显示机械倒计时。

2. `orient`
   - 用 2–3 句无剧透文字回答：
     - 你将进入怎样的世界；
     - 这本书主要在观察什么；
     - 阅读时可以先留意什么。

3. `introduce`
   - 只介绍两个必要操作。
   - Window：切换窗景 / 调整声音。
   - Vinyl：选择唱片 / 播放或暂停。
   - 使用真实控件的轻微高亮，不创建独立教程面板。

4. `begin`
   - 音乐和环境声平滑进入首个阅读阶段。
   - 显示“开始阅读”或具有书籍气质的等价短句。

### 引导约束

- 首次进入时显示；之后通过 `localStorage` 记住完成状态。
- 必须提供“跳过引导”。
- 设置区必须提供“重新播放引导”。
- 不得自动播放音频。首次用户点击“开始引导”后才允许淡入音频。
- `prefers-reduced-motion` 下取消呼吸缩放，仅保留透明度变化。
- 引导结束后不得遗留大面积遮罩或占据阅读空间。
- 引导文本必须面向未读者，禁止使用未解释的专有名词。

---

## 2.2 读前坐标

每本书必须提供简短、无剧透、面向未读者的读前坐标。

它不是剧情简介，而是进入阅读所需的最小理解框架。

必须包含：

```text
world       你将进入怎样的世界
concern     这本书主要在观察什么
notice      阅读时可以先留意什么
permission  允许读者暂时不理解什么
```

其中 `permission` 很重要，例如：

> 不必急着记住所有名字。先感受谁在说话、谁被迫沉默。

读前坐标在入静引导中首次出现，之后可以通过轻量入口重新查看。

验收要求：

- 每项尽量不超过 45 个汉字；
- 总计不超过 180 个汉字；
- 不剧透关键转折、凶手、结局或人物命运；
- 不假定读者已经看过本书；
- 不使用“本页将通过某某隐喻表达……”之类设计说明。

---

## 2.3 Window 模式

### 核心体验

书籍的章节或阅读阶段变成一组窗景。

每个窗景包含：

- 一张主画面；
- 天气类型和粒子参数；
- 光线与色调；
- 环境声；
- 一首阶段专属纯音乐 BGM；
- 2–4 条缓慢浮现的文字；
- 一条无剧透阅读提示。

用户的主要动作只有：

- 切换窗景；
- 开关或调整声音；
- 进入专注阅读；
- 可选记录一条边注。

### 视觉原则

- 画面是主角，UI 默认安静地贴近窗沿或屏幕边缘。
- 浮现文字一次只出现一小段，不形成信息墙。
- 天气必须持续存在，但不能遮挡文字。
- 切换窗景时使用光、雾、景深和声音交叉淡化，不做生硬幻灯片切换。
- 16:9 桌面屏幕中，主画面至少保留 70% 的视觉面积。

---

## 2.4 Vinyl 模式

### 核心体验

书籍的章节或阅读阶段变成唱片架上的多张唱片。

每张唱片包含：

- 唱片或封套图；
- 阶段名称；
- 一首纯音乐 BGM；
- 环境声组合；
- 2–4 条封套文字；
- 一条无剧透阅读提示；
- 与该阶段匹配的天气和灯光。

用户的主要动作只有：

- 从唱片架选择唱片；
- 播放或暂停；
- 调整音量或环境声；
- 进入专注阅读；
- 可选记录一条边注。

### 交互原则

- 选择唱片后，前一首淡出，后一首淡入。
- 唱片、唱针和转盘动画只服务于播放状态反馈。
- 不模拟复杂唱机机械结构，不要求 Three.js。
- 唱片架必须能在 16:9 中容纳多个阶段，并支持横向滚动或分页。
- 页面直接通过 `<audio src="...">` 或静态 JS 配置引用 BGM，不能依赖 `fetch()` 元数据后才发现本地音频。

---

## 2.5 章节与阅读阶段

默认生成 4–8 个阅读阶段，而不是强行按每一章生成。

### 默认模式：阅读阶段

当用户只提供书名，或无法可靠获取目录时：

- 根据书籍整体结构生成 4–8 个无剧透阅读阶段；
- 阶段名称表达阅读姿态或情绪变化；
- 不伪造真实章节标题。

### 精确章节模式

只有满足以下任一条件时才按章节生成：

- 用户主动提供目录；
- 能从可靠来源确认目录；
- 用户明确要求按章节。

若章节超过 8 个，将相邻章节合并为 4–8 个“唱片面”或“窗景组”。

---

## 2.6 阶段导演简报

每个阶段必须先生成一份短小但具体的“阶段导演简报”，再据此生成图片 Prompt、BGM Prompt、文字、天气、动画与 UI 变化。

它是新版 Skill 最重要的个性化中间产物。

### 三层设计系统

#### 稳定运行时层

所有阶段保持稳定：

- 控件的基本位置与操作方式；
- 键盘、触屏与无障碍行为；
- 音频切换、计时器、笔记和专注模式；
- Window 的窗景切换方式；
- Vinyl 的唱片选择和播放方式。

#### 书籍身份层

整本书共享：

- 核心张力与主要感官；
- 视觉母题与构图原则；
- 字体气质、材质和控件造型；
- 全局色彩范围；
- 音乐世界与主要乐器；
- 文字声线；
- 动画的总体性格；
- 明确禁止出现的通用审美。

#### 阶段变化层

每个阶段允许变化：

- 当前情绪位置与张力强度；
- 主画面与构图重心；
- 天气、光线和色温；
- BGM 的速度、密度、乐器占比和动态；
- 环境声；
- 浮现文字的长度、语气和出现节奏；
- UI 的局部颜色、材质状态、显隐密度和动画速度。

### 阶段导演简报内容

每个阶段至少定义：

```text
meaning        当前阶段在整本书中的意义，不剧透具体事件
emotion        当前情绪位置与强度
sensoryFocus   当前最重要的感官：光、雾、热、距离、噪声、触感等
visualBeat     当前阶段必须出现的精神画面
musicBeat      当前阶段音乐如何区别于前后阶段
textVoice      浮现文字的声线、句长和停顿方式
motionBeat     动画速度、方向与呼吸感
uiExpression   局部 UI 如何呼应该阶段
continuity     与前后阶段保持同一本书连续性的元素
avoid          当前阶段最容易落入的俗套
```

### 阶段区分原则

- 阶段之间必须有可感知差异，不能只替换图片和标题。
- 差异应来自书籍内部的情绪与思想变化，不来自随意更换视觉风格。
- 所有阶段仍应像同一本书：至少共享一种材质、一组字体关系、一个视觉母题和一个音乐母题。
- UI 可以阶段化，但只改变表达，不改变基本操作位置和含义。
- 不得让每个阶段像不同网站、不同应用或不同艺术家拼盘。

---

## 2.7 图片生成指导

图片不是普通背景资源，而是每个阅读阶段最主要的情绪载体。

新版 Skill 必须优先指导 Agent 为每个阶段分别生成图片，而不是只生成一张概念图反复换滤镜。

正式生成任务中，每个阶段必须对应一张专属图片。
只有图片生成能力不可用时，才允许使用明确标记的替代素材或 Prompt fallback。

### 每张阶段图片的生成依据

图片 Prompt 必须同时包含：

- 书籍身份层：共同视觉母题、材质、时代感、构图语言；
- 阶段导演简报：当前精神画面、情绪强度、感官重点与光线；
- 模式角色：
  - Window 图片是读者持续凝视的窗外世界；
  - Vinyl 图片是唱片封套、标签或阶段视觉身份；
- UI 使用要求：负空间、主体位置、裁切安全区和文字避让区；
- 禁止项：可读文字、Logo、通用封面排版、游戏 UI、廉价插画感。

### 图片 Prompt 结构

每个阶段生成并保存一份可审查的 Prompt，建议结构：

```text
Book identity:
Stage meaning:
Emotional position:
Central image:
Composition and safe areas:
Light, weather, materials:
Continuity with other stages:
What must visibly change in this stage:
Avoid:
```

### 图片组连贯性

- 先生成整本书的视觉母题，再生成各阶段图片。
- 各阶段图片必须共享明确的视觉 DNA，而不是完全不同的风格。
- Window 应像从同一个阅读空间望向逐渐变化的世界，除非书籍本身要求地点迁移。
- Vinyl 的封套可以差异更大，但必须像同一套唱片合集。
- 应记录每张图与当前阶段之间的具体对应关系，禁止只写“符合氛围”。

### 无生图能力时

本次测试使用历史图片，但仍必须：

- 为每个测试阶段写出完整阶段图片 Prompt；
- 选择最接近该阶段的历史图片；
- 记录“理想图片意图”和“测试替代素材”之间的差异；
- 验收重点放在运行时能否承载真正的多阶段图片，而不是历史图片是否完美匹配。

---

## 2.8 BGM 生成指导

BGM 不是一首贯穿全书的通用氛围音乐。
每个阶段都应拥有贴合当前阅读状态的纯音乐，并与前后阶段形成连续但可感知的变化。

正式生成任务中，每个阶段必须对应一首专属纯音乐 BGM。
生成失败时可以降级为环境声，但必须在元数据中标记失败，不得让所有阶段静默复用同一首通用 BGM。

### 全书音乐身份

生成阶段 BGM 前，先定义整本书共享的音乐身份：

- 主要乐器与禁止乐器；
- 速度范围；
- 音色质地；
- 空间感；
- 动态范围；
- 可重复出现的音乐母题；
- 阅读时必须避免的抢注意力元素。

### 每首阶段 BGM 的指导内容

每个 BGM Prompt 必须说明：

- 当前阶段的意义与情绪强度；
- 与上一阶段相比增加、减少或改变什么；
- 主要与次要乐器；
- 大致速度、节奏密度和动态；
- 环境空间与声音距离；
- 是否需要留白、循环友好和长时间阅读适配；
- 如何保留全书共享音乐母题；
- 严格纯音乐要求。

Prompt 必须带上：

```json
"is_instrumental": true
```

并明确：

```text
Strictly instrumental, no vocals, no singing, no spoken words, no lyrics.
```

### 阶段音乐差异

- 差异不能只来自“更悲伤 / 更快乐”这样的泛化词汇。
- 应具体改变速度、乐器层次、声场距离、节奏密度、和声张力或留白程度。
- 相邻阶段必须能平滑交叉淡化。
- 不同阶段不能像随机播放不同歌单；应保持共同母题或音色亲缘。
- 音乐必须适合持续阅读，避免强烈主旋律、突发音效和过度戏剧化高潮。

### 无音乐 API 时

本次开发使用历史 BGM，但仍必须：

- 为每个测试阶段写出理想 BGM Prompt；
- 从历史 BGM 中选择最接近的替代音轨；
- 在测试素材清单中说明选择理由；
- 验证多音轨切换、淡入淡出和阶段同步，而不是评价历史 BGM 是否完全符合新阶段。

---

## 2.9 阶段文字、动画与 UI 表达

### 阶段文字

每个阶段的浮现文字不是摘要，也不是随机金句。

它应承担三种功能：

- 帮读者感受当前阶段；
- 提醒可以留意的阅读线索；
- 给读者留下继续阅读的空间。

要求：

- 每阶段 2–4 条；
- 单条简短，一次只显示一条；
- 不剧透；
- 使用当前阶段导演简报定义的声线；
- 不同阶段在句长、停顿和语气上应有差异；
- 避免所有阶段都使用相同的抒情句式。

### 阶段动画

动画必须来自阶段情绪，而不是统一套用淡入淡出：

- 平静阶段可以缓慢呼吸、漂移和长停顿；
- 紧张阶段可以缩短停顿、收紧光区、增加细微抖动或风压；
- 迷离阶段可以使用轻微失焦、雾化和残影；
- 开阔阶段可以扩大景深、拉长声音和光线移动。

仍须保持克制，并支持减少动态效果。

### 阶段 UI

允许每个阶段改变 UI 的表达，例如：

- 色彩、光泽、纸张或金属质感；
- 边缘磨损、雨痕、雾气、唱片标签颜色；
- 字体大小关系与文字出现方式；
- 控件显隐密度；
- 局部装饰和选中状态动画。

禁止改变：

- 核心控件位置到用户无法预期；
- 相同操作的名称和含义；
- 键盘与触屏行为；
- 基础可读性和对比度；
- 为追求阶段差异而重新设计整套页面。

---

## 2.10 正式生成顺序

后续真正为一本书生成页面时，必须按照以下顺序工作：

```text
理解书籍与读者需要
→ 划分阅读阶段
→ 定义整本书的视觉、音乐、文字与动画身份
→ 为每个阶段写导演简报
→ 生成并审查整组图片 Prompt
→ 生成并审查整组 BGM Prompt
→ 生成阶段图片与阶段 BGM
→ 写阶段文字、天气、动画和局部 UI 表达
→ 加载 Window / Vinyl 稳定运行时
→ 根据真实视听资产完成主题 CSS 与最终验收
```

禁止先完成一套通用 UI，再为它随意填充图片和音乐。
图片与 BGM 是新版的核心创作产物；UI 的职责是托住并放大它们。

---

## 3. 建议代码架构

改造后，Coding AI 不应每次重写完整页面，而应加载预制运行时组件并生成书籍主题配置。

建议目录：

```text
vibereading-skill/
├── SKILL.md
├── runtime/
│   ├── shared/
│   │   ├── companion-controls.js
│   │   ├── guided-entry.js
│   │   ├── audio-controller.js
│   │   ├── weather-engine.js
│   │   └── runtime.css
│   ├── window/
│   │   ├── index.html
│   │   ├── window-runtime.js
│   │   └── window.css
│   └── vinyl/
│       ├── index.html
│       ├── vinyl-runtime.js
│       └── vinyl.css
├── references/
│   ├── template-index.md
│   ├── templates/
│   │   ├── window.md
│   │   └── vinyl.md
│   ├── companion-manifest-schema.md
│   ├── stage-direction.md
│   ├── image-generation.md
│   ├── music-generation.md
│   ├── audio-manifest.json
│   ├── effects-recipes.md
│   ├── note-share-export.js
│   └── tests/
└── output/
```

如果执行过程中发现更简单的结构，可以调整，但必须满足：

- Window 与 Vinyl 共享音频、天气、引导和陪伴控件逻辑；
- 两个模板不得复制一整套相同代码；
- 每次生成主要写配置、文字和主题变量，而不是重新发明运行时；
- 每次生成的主要创作工作是形成书籍身份与阶段导演简报，并据此生成阶段图片、BGM、文字和主题变化；
- 输出仍然可以作为静态页面独立运行；
- 输出不能依赖构建工具、后端服务或在线 CDN。

### 输出方式

每次生成任务应：

1. 复制选中模式的预制运行时文件；
2. 复制共享运行时文件；
3. 写入用于审查和校验的 `companion-manifest.json`；
4. 同步写入内容一致的 `companion-manifest.js`，以静态脚本形式暴露配置；
5. 运行时直接读取 `companion-manifest.js`，不得通过 `fetch()` 才加载核心配置；
6. 写入书籍专属主题 CSS；
7. 复制实际使用的图片、环境声和 BGM；
8. 保存每个阶段的图片 Prompt 与 BGM Prompt，便于人工审查和失败重试；
9. 生成薄层 `index.html`，加载运行时与配置。

`companion-manifest.json` 用于 Agent、人工审查和校验器；`companion-manifest.js` 用于浏览器运行。
二者必须由同一份数据生成，并由校验器检查内容一致性。这样既保留结构化产物，也能保证直接双击 `index.html` 时不被 `file://` 的跨域限制阻塞。

---

## 4. 新配置结构

用 `companion-manifest.json` 替代当前复杂的 `space-spec.json`。

建议结构：

```json
{
  "book": {
    "title": "",
    "author": "",
    "orientation": {
      "world": "",
      "concern": "",
      "notice": "",
      "permission": ""
    }
  },
  "mode": "window|vinyl",
  "bookDirection": {
    "visualIdentity": "",
    "musicIdentity": "",
    "textVoice": "",
    "motionCharacter": "",
    "sharedMotifs": [],
    "avoid": []
  },
  "guidedEntry": {
    "settle": ["", ""],
    "beginLabel": "",
    "duration": "short"
  },
  "stages": [
    {
      "id": "",
      "label": "",
      "range": "",
      "readingHint": "",
      "floatingTexts": ["", ""],
      "direction": {
        "meaning": "",
        "emotion": "",
        "sensoryFocus": "",
        "visualBeat": "",
        "musicBeat": "",
        "textVoice": "",
        "motionBeat": "",
        "uiExpression": "",
        "continuity": "",
        "avoid": ""
      },
      "image": "./assets/images/...",
      "imagePrompt": "./prompts/[stage-id]-image.md",
      "bgm": "./assets/audio/...",
      "bgmPrompt": "./prompts/[stage-id]-bgm.md",
      "ambience": ["./assets/audio/..."],
      "weather": {
        "kind": "",
        "intensity": "quiet|medium",
        "color": ""
      },
      "light": {
        "tone": "",
        "brightness": ""
      },
      "ui": {
        "accent": "",
        "surface": "",
        "density": "quiet|balanced",
        "transition": ""
      }
    }
  ],
  "theme": {
    "palette": {},
    "materials": [],
    "motion": ""
  },
  "features": {
    "notes": true,
    "timer": true,
    "focus": true,
    "guidedEntryReplay": true
  }
}
```

最终字段可在实现中微调，但必须保持短小，且所有字段都直接影响页面。
阶段导演简报属于必要内容，不得为了压缩 JSON 而删除；应压缩的是重复说明和不影响实现的抽象修辞。

禁止重新加入：

- 签名拓扑；
- 关系网络；
- 谜题完成条件；
- 三幕协议映射；
- 大段艺术指导说明；
- 与运行时无关的抽象描述。

---

## 5. 预制共享控件

共享陪伴控件必须提前实现，后续生成任务只配置是否启用。

最低能力：

- 当前阶段显示与切换；
- BGM 播放 / 暂停；
- 环境声开关与音量；
- 阅读计时器：暂停 / 继续 / 重置；
- 专注模式；
- 简单笔记与 Markdown 导出；
- 读前坐标重看；
- 入静引导重播。

设计要求：

- 默认折叠或弱显示，不抢画面；
- Window 中贴近窗沿；
- Vinyl 中融入唱机控制区或封套；
- 不生成占据屏幕三分之一以上的通用控制面板；
- 所有按钮有可见焦点状态和至少 44px 的触摸区域；
- 所有功能在键盘和触屏上可用；
- 控件文字使用中文。

---

## 6. 离线测试素材

本任务不具备生图 Skill 和音乐 API key。使用以下历史产物进行开发与验收。

### 6.1 Window 测试图片

优先使用：

```text
output/2026-05-11-瓦尔登湖-自然静谧场域测试/concept-image.png
output/2026-05-10-我的阿勒泰-field模板全功能测试/concept-image.png
output/2026-05-20-全世界的水都会重逢-route/concept-image.png
output/2026-05-12-红楼梦-阅读引导页面测试/concept-image.png
```

用途：

- 前三张用于测试多窗景切换和 16:9 裁切；
- 《红楼梦》图片用于测试非自然风景画面是否仍能形成克制窗景。

### 6.2 Vinyl 测试图片

优先使用：

```text
output/2026-05-12-挪威的森林-vinyl测试/concept-image.png
output/2026-05-09-道德经-抽象哲学模板差异测试/concept-image.png
output/2026-05-10-白噪音-signal模板回归测试/concept-image.png
```

用途：

- 《挪威的森林》作为封套与唱片视觉基准；
- 另外两张用于测试同一唱片架中不同阶段封套的差异。

### 6.3 Vinyl 多唱片 BGM

从以下路径复制为 4–6 个测试音轨：

```text
output/2026-05-12-挪威的森林-vinyl测试/assets/audio/bgm.mp3
output/2026-05-11-瓦尔登湖-自然静谧场域测试/assets/audio/bgm.mp3
output/2026-05-12-红楼梦-阅读引导页面测试/assets/audio/bgm.mp3
output/2026-05-20-全世界的水都会重逢-route/assets/audio/bgm.mp3
output/2026-06-11-《玫瑰的名字》-精简流程实跑/assets/audio/bgm.mp3
```

### 6.4 环境声

从 `assets/audio/` 和 `references/audio-manifest.json` 中选择已有环境声。

不要复制全部音频。每个测试输出只复制实际引用的文件。

### 6.5 测试素材处理规则

- 只将历史素材复制进新的测试输出目录。
- 不修改历史输出。
- 不将大体积图片与 BGM 提交进 Git。
- 在测试输出的 `companion-manifest.json` 中明确记录实际使用路径。
- 为每个阶段额外写入理想图片 Prompt 和 BGM Prompt，即使实际使用的是历史替代素材。
- 在测试素材清单中记录每个历史素材对应哪个阶段、为什么选择、与理想方向有何差距。
- 如某个指定素材不存在，跳过该素材并使用列表中的下一个，不得因此停止。

### 6.6 测试输出中的阶段创作包

每个测试阶段至少包含：

```text
prompts/[stage-id]-direction.md
prompts/[stage-id]-image.md
prompts/[stage-id]-bgm.md
assets/images/[stage-id].png
assets/audio/[stage-id]-bgm.mp3
```

其中历史图片和 BGM 是运行时替代素材，Prompt 与导演简报才是未来真实生成时的创作依据。

---

## 7. 分阶段实施计划

## 里程碑 A：建立可回归基线

目标：在改造前记录现状，并建立新结构测试入口。

任务：

1. 确认当前分支和工作区状态。
2. 运行现有测试并记录结果。
3. 创建两个新的忽略输出：
   - `output/双模式-window-runtime-test/`
   - `output/双模式-vinyl-runtime-test/`
4. 从历史输出复制本文指定的离线素材。
5. 创建简短的本地素材清单，记录素材的书籍、色调、构图、音乐气质和适合模拟的阶段。
6. 为两个测试输出各设计一份书籍身份与至少 4 份阶段导演简报。

验收：

- 不修改历史输出；
- 两个测试目录具备至少 3 张图片和 4 首 BGM；
- 每个测试阶段都有图片 Prompt、BGM Prompt 与替代素材选择理由；
- 当前测试结果被记录在计划执行日志中。

建议 commit：

```text
chore: establish two-mode redesign baseline
```

## 里程碑 B：实现共享运行时

目标：先让共享能力真实可用，再处理两个视觉模式。

任务：

1. 实现 `guided-entry.js`：
   - 四步入静流程；
   - 跳过、重播、首次完成记忆；
   - 用户手势后才允许播放音频。
2. 实现 `audio-controller.js`：
   - BGM 播放、暂停、音量、淡入淡出；
   - 环境声独立控制；
   - 切换阶段时交叉淡化；
   - 直接支持 `file://`，不得依赖 `fetch()` 才发现音频路径。
3. 实现 `weather-engine.js`：
   - 至少支持雨、雪、雾、尘、风；
   - 强度可配置；
   - 降低动态效果支持。
4. 实现 `companion-controls.js`：
   - 阶段切换、计时器、专注、笔记导出、引导重播。
5. 实现共享 CSS。

验收：

- 共享组件没有模式专属布局；
- 音频在 `file://` 与 HTTP 下都能使用；
- 计时器和笔记功能有自动测试；
- 入静引导可以跳过和重播；
- 不自动播放音频。

建议 commit：

```text
feat: build shared companion runtime
```

## 里程碑 C：实现 Window 运行时

目标：完成稳定、简洁、以画面和文字为主的视觉陪伴模式。

任务：

1. 使用 3–5 张历史概念图建立多窗景切换。
2. 每个窗景先建立阶段导演简报，再配置独立图片意图、天气、光线、环境声、浮现文字、动画与局部 UI 表达。
3. 接入共享入静引导和陪伴控件。
4. 实现窗景切换过渡。
5. 确保文字一次只浮现一小段。
6. 验证各窗景有明显阶段差异，同时共享同一本书的视觉母题。
7. 建立桌面与移动端布局。

验收：

- 16:9 的 1366×768、1440×900、1920×1080 均无关键裁切；
- 主画面占据主要视觉面积；
- 切换窗景后图片、天气、声音、文字同步变化；
- 每个窗景的文字声线、动画节奏和局部 UI 表达符合阶段导演简报；
- 各窗景有可感知差异，但不会像不同网站拼接；
- 页面可在 `file://` 下运行；
- 入静引导结束后 UI 自动退居边缘。

建议 commit：

```text
feat: build window companion runtime
```

## 里程碑 D：实现 Vinyl 运行时

目标：完成多唱片选择与章节 BGM 切换。

任务：

1. 使用至少 4 首历史 BGM 建立多唱片架。
2. 使用至少 3 张历史概念图建立不同唱片封套。
3. 为每张唱片先建立阶段导演简报、理想图片 Prompt 与理想 BGM Prompt。
4. 实现唱片选择、播放、暂停、音量和音轨淡入淡出。
5. 唱片切换时同步改变灯光、天气、浮现文字、动画节奏、局部 UI 和阅读提示。
6. 接入共享入静引导和陪伴控件。
7. 建立桌面与移动端布局。

验收：

- 同时可发现多张唱片；
- 用户可手动切换；
- 切换后旧音轨停止，新音轨播放；
- 每张唱片有明确的阶段音乐差异，同时能听出共享的全书音乐身份；
- 封套、音乐、文字、天气和局部 UI 表达属于同一个阶段方向；
- 不出现多个 BGM 同时播放；
- 16:9 中唱机、唱片架和必要控件完整可见；
- 页面可在 `file://` 下运行；
- 唱机动画不阻塞阅读，也不要求 Three.js。

建议 commit：

```text
feat: build vinyl companion runtime
```

## 里程碑 E：收缩 Skill 与文档

目标：让后续 Coding AI 只需要理解两个模式和短配置。

任务：

1. 重写 `SKILL.md`：
   - 核心目标改为陪伴；
   - 工作流改为选择 Window / Vinyl；
   - 生成读前坐标和 4–8 个阶段；
   - 强制先形成书籍身份与阶段导演简报，再生成逐阶段图片和 BGM；
   - 将逐阶段图片与 BGM 设为主要生成产物，UI 实现排在其后；
   - 复制预制运行时；
   - 无 API 时使用 fallback 素材或写清资源状态；
   - 删除复杂互动和签名机制要求。
2. 将 `template-index.md` 收缩为两个模式。
3. 重写 `window.md` 和 `vinyl.md`。
4. 从活跃 `references/templates/` 删除其余六个模板。
5. 用 `companion-manifest-schema.md` 替代 `space-spec-schema.md`。
6. 新增精简的 `stage-direction.md`，指导书籍身份与阶段导演简报。
7. 重写 `image-generation.md`，重点说明阶段图片 Prompt、整组连贯性与模式图像角色。
8. 重写 `music-generation.md`，重点说明全书音乐身份、逐阶段 BGM Prompt、纯音乐约束和阶段连续性。
9. 收缩不再适用的效果文档。
10. 更新根目录 `AGENTS.md` 与 `CLAUDE.md` 中的项目结构和工作流说明。

注意：

- 不要把六个旧模板移动到 Skill 内的 `legacy/`，避免继续占据 Agent 上下文。Git 历史已足够保留它们。
- 如果根目录文档位于嵌套 Git 仓库之外，可以修改并在最终报告中明确说明它们未包含于嵌套仓库 commit。

验收：

- 活跃模板恰好只有 `window` 与 `vinyl`；
- 普通生成流程不再读取复杂旧模板；
- Skill 文档以中文为主；
- 通用文档总量明显下降；
- 后续生成主要写书籍身份、阶段导演简报、Prompt、manifest 和主题内容；
- 图片与 BGM 指导是工作流重点，而不是附属步骤。

建议 commit：

```text
refactor: reduce skill to two companion modes
```

## 里程碑 F：重写校验器与测试

目标：让验收围绕“陪伴是否稳定好用”，而不是复杂互动。

任务：

1. 重写输出校验器，检查：
   - `companion-manifest.json`；
   - `companion-manifest.js` 与 JSON 内容一致；
   - 模式只能为 `window` 或 `vinyl`；
   - 读前坐标四项完整；
   - 阶段数量 4–8；
   - 每阶段具备导演简报、图片 Prompt、BGM Prompt、文字、天气、动画和局部 UI 配置；
   - 所有阶段具备共享书籍身份；
   - 相邻阶段的视觉与音乐指导不能完全相同；
   - BGM 路径真实存在；
   - Window 多窗景可切换；
   - Vinyl 多唱片可切换；
   - 入静引导、跳过和重播能力存在；
   - `file://` 不依赖运行时 `fetch()` 加载核心配置或 BGM；
   - 无自动播放；
   - 支持减少动态效果。
2. 为共享运行时增加 Node 单元测试。
3. 为 Window 与 Vinyl 增加浏览器 smoke 测试说明或自动化脚本。
4. 删除不再适用的拓扑、关系、谜题和 Three.js 校验。

验收：

- 两个离线测试输出均通过新校验器；
- 故意删除一个阶段音频时校验器会失败；
- 故意移除读前坐标时校验器会失败；
- 故意让所有阶段复用同一份导演简报或 Prompt 时，校验器会失败或明确警告；
- 故意恢复自动播放时校验器会失败或明确警告。

建议 commit：

```text
test: validate two companion modes
```

## 里程碑 G：最终视觉与行为验收

目标：完成真实使用场景下的最终复查。

必须分别测试 Window 与 Vinyl。

桌面尺寸：

```text
1366 × 768
1440 × 900
1920 × 1080
```

移动尺寸：

```text
390 × 844
430 × 932
```

每个模式必须走通：

```text
首次打开
→ 开始或跳过入静引导
→ 理解读前坐标
→ 进入首个阅读阶段
→ 切换至少两个阶段
→ 播放 / 暂停声音
→ 进入专注模式
→ 写入并导出一条笔记
→ 重播入静引导
```

视觉验收重点：

- 没有上下塞满、关键内容被裁切的问题；
- 画面与声音是主角，控件退居次要位置；
- 文字具有感染力，但没有连续大段说明；
- 每个阶段的图片、BGM、天气、文字、动画和局部 UI 共同表达同一阶段；
- 阶段差异明显，但仍保持同一本书的视觉与音乐身份；
- 入静引导简洁，不像产品 onboarding；
- 未读者能够理解页面；
- Window 与 Vinyl 明显不同，但共享能力一致；
- 没有旧式复杂模板残留。

建议 commit：

```text
fix: complete two-mode visual acceptance
```

---

## 8. 自动验收命令

执行 Agent 应根据最终实现补齐实际命令，但至少保持以下入口：

```powershell
node --check runtime/shared/guided-entry.js
node --check runtime/shared/audio-controller.js
node --check runtime/shared/weather-engine.js
node --check runtime/shared/companion-controls.js
node --check runtime/window/window-runtime.js
node --check runtime/vinyl/vinyl-runtime.js

node --test references/tests/*.test.js

node references/tests/evaluate-vibereading-output.mjs "output/双模式-window-runtime-test"
node references/tests/evaluate-vibereading-output.mjs "output/双模式-vinyl-runtime-test"

git diff --check
git status --short
```

如果测试文件或命令名称在实现中调整，必须同步更新本节或新增 `references/tests/README.md`。

---

## 9. 质量门槛

以下任一情况存在，任务都不能宣告完成：

- 仍有超过两个活跃模板；
- 生成流程仍要求每本书发明复杂交互；
- 入静引导不能跳过或重播；
- 页面假定用户已经读过本书；
- 读前坐标包含剧透或未解释专有名词；
- 16:9 下关键内容被裁切；
- 核心功能依赖 HTTP 服务，直接双击页面失效；
- BGM 依赖运行时 `fetch()` 元数据后才被发现；
- 切换唱片时多个 BGM 同时播放；
- Window 只是静态壁纸；
- Vinyl 只是装饰唱机，没有多唱片切换；
- 所有阶段只是复用同一张图片、同一首 BGM 或同一种文字动画；
- 阶段图片、BGM、文字与 UI 各说各话，没有共同导演方向；
- 每个阶段为了求差异而变成完全不同的网站；
- 图片 Prompt 与 BGM Prompt 没有保存为可审查产物；
- 控件占据主要视觉面积；
- 缺少天气效果；
- 音频自动播放；
- 缺少键盘、触屏或减少动态效果支持；
- 新 Skill 文档仍然过长、重复或主要使用英文；
- 测试未通过；
- 工作区存在未解释的已跟踪修改。

---

## 10. 最终交付报告格式

全部完成后，执行 Agent 必须输出一份中文报告，包含：

1. 最终分支名与 commit 列表；
2. 新架构说明；
3. Window 与 Vinyl 的主要体验；
4. 入静引导与读前坐标如何实现；
5. 使用了哪些历史测试素材；
6. 自动测试结果；
7. 浏览器验收尺寸与结果；
8. 尚存风险；
9. 与旧 Skill 相比删除了什么、保留了什么；
10. 所有重要文件的路径。

不要只说“已完成”。必须提供可复查证据。

---

## 11. 默认决策

遇到歧义时使用以下默认值，不要停下来询问：

- 默认生成 5 个阅读阶段；
- 默认使用 Window，只有书籍明显由音乐、节奏、声音记忆或用户明确要求时选择 Vinyl；
- 默认首次显示入静引导，之后记住完成状态；
- 默认启用计时器、专注、声音控制和简短笔记；
- 默认浮现文字每阶段 3 条；
- 默认每个阶段都生成独立图片 Prompt、BGM Prompt 和阶段导演简报；
- 默认 UI 的基本结构稳定，但允许阶段化改变色彩、材质状态、显隐密度和动画节奏；
- 默认所有阶段共享至少一个视觉母题和一个音乐母题；
- 默认不显示真实章节号，除非目录可靠；
- 默认不使用 Three.js；
- 默认使用 DOM/CSS 和少量 Canvas 粒子；
- 默认保证 `file://` 可运行；
- 默认优先修复稳定性与可理解性，而不是增加视觉复杂度。
