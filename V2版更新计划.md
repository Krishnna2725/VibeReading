# VibeReading V2 正式实施计划

## 0. 本文地位

本文是 VibeReading V2 的唯一实施计划。

V2 不兼容旧版的签名机制、交互拓扑、关系网络、解谜流程，以及强制的 `entry / exploration / companion` 三状态。
旧版内容由 Git 历史保留；实施 V2 时应彻底删除不再适用的文档、字段、模板说明和校验规则，不得让新旧契约同时影响 Agent。

---

## 1. 产品定义

VibeReading V2 为一本书生成一个高质量的 **16:9 桌面阅读陪伴空间**。

每次打开或刷新页面，读者都会回到首页，并通过一次 15–25 秒的短暂入静引导开始阅读。
引导类似简短冥想：画面、天气、文字和声音共同帮助读者安静下来，理解这本书的基本气质，然后进入可长期停留的阅读陪伴空间。

页面的价值优先级：

```text
1. 书籍个性与整体氛围
2. 主视觉 / 背景图
3. BGM 与必要环境音
4. 天气 / 氛围效果
5. 入静引导
6. 阅读阶段切换
7. 阅读计时与番茄钟
8. 少量模板化交互
```

页面不是复杂互动装置、剧情导览、解谜游戏、电子书阅读器或生产力仪表盘。

核心原则：

> V2 不是减少创造力，而是把创造力从复杂交互转移到书籍个性、主视觉、声音、天气、文字与模板主体上。

---

## 2. 已确定的产品决策

### 2.1 五个模板平等使用

V2 保留五个平等模板：

```text
window
vinyl
instrument
route
symbols
```

不设默认模板与条件模板的层级。Agent 根据书籍最合适的读前入口选择一个模板。

彻底删除：

```text
archive
rehearsal
labyrinth
oracle
```

`oracle` 由无占卜逻辑的 `symbols` 取代。

### 2.2 每次打开都重新入静

- 页面加载或刷新后始终回到入静首页。
- 不使用 `localStorage` 跳过后续访问的入静引导。
- 可以提供“跳过引导”，但刷新后仍再次出现。
- 页面内部发生异常时，用户刷新即可安全回到首页。

### 2.3 声音是入静引导的必要部分

每个页面必须具备一首定制纯音乐 BGM。
必要时可以同时使用环境音。

浏览器通常禁止在用户零操作时播放有声音频，因此统一采用：

```text
页面加载
→ 显示极简入静首页
→ 用户点击“开始”
→ 立即启动 BGM 或环境音
→ 同时开始入静引导
```

硬要求：

- 用户点击开始后，入静阶段必须有声音；
- 优先播放 BGM；
- BGM 未就绪或播放失败时，必须立即使用环境音兜底；
- 不允许无声完成入静引导；
- BGM 不得包含歌词、人声、朗诵或口白；
- 页面内声音默认持续播放，用户可以关闭或调整；
- 不设计多 BGM 切换。

### 2.4 一书一首 BGM

所有模板每本书只生成一首 BGM。

包括 Vinyl 在内，不再为不同阶段生成多首 BGM，也不再构建多唱片多音轨系统。
阶段变化主要通过图片、天气、环境声、文字、光线、动画与局部 UI 表达。

### 2.5 默认四个阅读阶段

每个页面默认生成四个无剧透阅读阶段。

阶段用于切换阅读气氛和陪伴提示，不模拟真实阅读进度，也不解释剧情。
如用户明确提供目录，可以让阶段对应章节组；否则使用阅读姿态或情绪阶段。

### 2.6 图片资源预算

只有 Window 生成多张主图：

```text
1 张完整窗边阅读空间母图
+ 最多 3 张基于母图的图生图变化
= 最多 4 张
```

变化主要修改窗外风景、天气、季节、时间和光线。
必须保持室内空间、窗户结构、观察视角与整体视觉身份连续。

其他模板默认只生成一张主要视觉资源：

- Vinyl：一张专辑封套 / 唱片 label 图；
- Instrument：一张背景环境、仪器表面或屏幕纹理；
- Route：一张旅途空间；
- Symbols：一张仪式表面或象征空间。

### 2.7 一级能力

每个模板必须提供以下一级能力：

```text
1. 入静引导
2. 天气 / 氛围控制
3. 声音控制
4. 四阶段切换
5. 阅读计时与番茄钟
```

其他能力属于二级能力，例如：

- 重新查看读前坐标；
- 专注模式；
- 简短帮助；
- 可选笔记。

一级能力应自然融入模板预制陪伴组件，不应散落成大型通用面板。

---

## 3. 读前坐标

每本书必须提供简短、无剧透、面向未读者的读前坐标。

它不是剧情简介，而是进入阅读所需的最小理解框架：

```text
world       你将进入怎样的世界
concern     这本书主要在观察什么
notice      阅读时可以先留意什么
permission  允许读者暂时不理解什么
```

要求：

- 每项尽量不超过 45 个汉字；
- 总计不超过 180 个汉字；
- 不剧透关键转折、结局或人物命运；
- 不假定读者已经读过；
- 避免未解释的专有名词；
- 不写设计说明或模板说明。

读前坐标主要在入静引导中自然出现，进入陪伴空间后可通过二级入口重新查看。

---

## 4. 入静引导

入静引导是每次阅读开始前的短暂冥想，不是产品教程。

### 4.1 默认流程

总时长 15–25 秒：

```text
settle      让身体和视线慢下来
orient      用读前坐标建立最小理解
attune      让声音、天气和画面完成对齐
begin       进入阅读陪伴
```

不要求逐项介绍所有功能。
功能应通过页面中清晰、简单的控件自然被理解。

### 4.2 模板骨架与个性化

四步节奏可以作为共享骨架，但每次生成的文字、声音、画面变化和动画必须贴合书籍与模板。

示例：

- Window：窗外光线变亮，天气逐渐显现；
- Vinyl：设备通电，唱片开始旋转，唱针落下；
- Instrument：噪声降低，核心信号稳定；
- Route：远方与路线缓慢显现；
- Symbols：象征物从雾、影或光中显形。

### 4.3 声音规则

- 首页展示“开始”动作；
- 用户点击开始后立即尝试播放 BGM；
- 如果 BGM 不可用，立即播放环境音；
- 引导期间至少一种声音持续存在；
- 声音使用缓慢淡入；
- 不等待远程音频生成完成才构建页面。

### 4.4 约束

- 必须提供跳过；
- 刷新后重新回到入静首页；
- 不保存已经完成引导的状态；
- 不出现机械倒计时；
- 不形成大段教程文字；
- 不剧透；
- `prefers-reduced-motion` 下取消呼吸缩放和复杂运动，只保留淡入淡出；
- 引导结束后遮罩和引导 UI 必须彻底退出。

---

## 5. 书籍身份与阅读阶段

### 5.1 书籍身份

生成页面前，先定义整本书共享的身份：

```text
visualMotif       视觉母题与构图原则
musicDirection    一首 BGM 的音乐方向
textVoice         文字语气与句长
motionCharacter   动画总体性格
uiLanguage        字体、材质与控件表达
avoid             最容易滑向的俗套
```

### 5.2 四个阶段

每个阶段定义：

```text
label             简短阶段名
readingHint       无剧透阅读提示
floatingTexts     2–4 条短文字
weather           天气与强度
light             光线与色温
ambience          可选环境声变化
motion            动画节奏
uiAccent          局部 UI 表达变化
image             Window 专用的阶段图片
```

四个阶段必须有可感知变化，但仍属于同一本书。
阶段差异不能只替换标题，也不能变成四个不同网站。

除 Window 外，其他模板主要通过天气、光线、环境声、文字、动画和局部 UI 表达阶段变化。

---

## 6. 五个模板

每个模板文档必须独立定义：

```text
Role
Best For
Avoid
Primary Production Mode
Creative Budget
Visual Preset
BGM Preset
Weather Preset
Companion Preset
Entry Guide Behavior
Stage Expression
Forbidden
Acceptance
```

## 6.1 Window

定位：图像主导的窗边阅读空间。

主生产方式：

- 生图生成完整窗边空间、窗户结构、室内光线和窗外风景；
- 使用母图进行最多三次图生图，表达四个阅读阶段；
- 前端只叠加天气、轻量文字、入静引导和毛玻璃陪伴组件；
- 前端不得重新绘制复杂窗框、窗台或大型装饰 SVG。

陪伴组件：`Frosted Glass Panel`。

创造力预算重点：

- 四张连续但不同的窗景；
- 天气与光线转换；
- 不遮挡画面的文字与毛玻璃面板。

## 6.2 Vinyl

定位：前端对象主导的复古唱机阅读空间。

主生产方式：

- 以《挪威的森林》历史输出的唱机方向作为质量参考；
- 预制唱机主体、唱片、唱臂、播放状态和旋钮逻辑；
- 生图生成一张贴合书籍的专辑封套 / label；
- 全书只播放一首 BGM；
- 四阶段改变 Liner Notes、天气、灯光、文字和局部材质，不更换 BGM。

陪伴组件：`Deck Controls`。

创造力预算重点：

- 书籍专属封套与硬件材质；
- 落针、旋转和声音反馈；
- 四阶段 Liner Notes 与灯光变化。

不再实现多唱片架、多音轨或拖动多张唱片播放。

## 6.3 Instrument

定位：一台明确、可理解的书籍主题仪器。

主生产方式：

- 默认 DOM / CSS / Canvas；
- Three.js 或 CSS 3D 仅在能显著增强仪器质感和核心动作时使用；
- 只允许一个核心仪器与少量可理解控件；
- 参数主要映射阶段、声音、天气和阅读提示。

陪伴组件：`Control Surface`。

创造力预算重点：

- 仪器材质和物理反馈；
- 一个清晰的书籍主题参数隐喻；
- 不超过五个主要控件。

禁止 NASA 控制室、数据仪表盘和无意义机械堆料。

## 6.4 Route

定位：图像与前端路径混合的旅途陪伴空间。

主生产方式：

- 生图生成一张旅途空间；
- 前端生成清晰、稀疏的四阶段路线；
- 切换站点时改变天气、光线、环境声、文字和路径高亮；
- 不制作真实地图或复杂寻路。

陪伴组件：`Wayfinder`。

创造力预算重点：

- 旅途空间与方向感；
- 四个停靠阶段；
- 路线和天气变化。

## 6.5 Symbols

定位：以少量象征物提供阅读观察视角的诗性陪伴空间。

主生产方式：

- 生图生成宁静的象征空间或表面；
- 前端生成 3–5 个始终可见的象征物；
- 四阶段改变被强调的象征、天气、文字、光线和声音；
- 象征物用于提示读者观察什么，不产生正确答案，不解锁内容。

陪伴组件：`Symbol Tokens`。

创造力预算重点：

- 书籍专属象征物；
- 象征与阅读提示之间的对应；
- 克制的显影、光线与文字。

禁止占卜、抽牌算命、随机预言、猜意义、牌阵关系和神秘学小游戏。

---

## 7. 预制与生成的边界

### 7.1 预制共享能力

新版应预制：

- 入静引导基础控制器；
- 声音控制器；
- BGM 失败时的环境音兜底；
- 天气引擎；
- 四阶段切换基础逻辑；
- 阅读计时与番茄钟逻辑；
- `file://` 兼容加载方式；
- 可访问性与减少动态效果基础能力。

### 7.2 预制模板能力

每个模板预制自己的陪伴组件骨架：

```text
Window      Frosted Glass Panel
Vinyl       Deck Controls
Instrument  Control Surface
Route       Wayfinder
Symbols     Symbol Tokens
```

Vinyl 的唱机主体应高度预制。
其他模板保留更多书籍专属视觉实现空间。

### 7.3 Coding AI 的主要职责

每次生成时，Coding AI 负责：

- 理解书籍并生成读前坐标；
- 选择一个模板；
- 定义书籍身份与四阶段；
- 生成一张主图；Window 额外生成最多三张图生图变化；
- 生成一首 BGM；
- 选择必要环境音；
- 将书籍个性转译为天气、文字、动画、材质和局部 UI；
- 加载并主题化预制能力；
- 完成 16:9、声音和 `file://` 验收。

---

## 8. 一级能力与陪伴组件

每个模板的陪伴组件必须包含：

```text
入静引导入口 / 重新开始
天气强度：关闭 / 低 / 中
声音：播放 / 暂停 / 音量
四阶段切换
阅读计时与番茄钟
```

计时器要求：

- 显示累计阅读时间；
- 支持暂停、继续和重置；
- 支持普通计时与番茄钟；
- 番茄钟默认 25 分钟；
- 不使用复杂图表或生产力仪表盘；
- 视觉表达必须符合模板。

陪伴组件约束：

- 一级能力可以超过四项，但必须组织清晰、默认安静；
- 任何一级能力最多两步可达；
- 不遮挡首屏主体；
- 不使用大型 SVG 装饰；
- 不使用玩具感控件；
- 文字标签优先于难懂图标；
- 必须支持键盘与触屏。

---

## 9. 图片生成

### 9.1 通用要求

- 默认 16:9；
- 主体完整；
- 明确 UI 安全留白；
- 不包含可读文字、Logo 或生成式 UI；
- 不生成大块不可控装饰；
- 图片必须贴合书籍身份和模板图像角色。

### 9.2 Window 图生图流程

```text
1. 生成完整窗边空间母图；
2. 确认室内结构、视角、主体与安全留白；
3. 基于母图进行最多三次图生图；
4. 只改变窗外风景、天气、季节、时间和光线；
5. 检查四张图切换时空间结构保持稳定。
```

不得并发生成四张互不相关的窗景。

### 9.3 其他模板

Vinyl、Instrument、Route、Symbols 默认只生成一张主要图像资源。
阶段变化主要由前端氛围表达完成。

---

## 10. BGM 与环境音

### 10.1 BGM

每本书生成一首严格纯音乐 BGM。

要求：

- 请求必须包含 `"is_instrumental": true`；
- Prompt 描述书籍气质、速度、乐器、动态和阅读适配；
- 不包含人声、演唱、口白、歌词或 vocal pads；
- BGM 生成异步进行，不阻塞页面主体；
- 最终状态只能为 `generated / skipped / failed`，不得交付 `pending`；
- 页面只引用一条 `./assets/audio/bgm.mp3`。

### 10.2 环境音

环境音用于：

- BGM 失败时保证入静引导有声音；
- 增强天气和空间；
- 必要时随阶段改变。

只复制实际使用的环境音。

### 10.3 播放规则

- 页面零操作时展示开始首页；
- 用户点击开始后立即启动 BGM；
- BGM 不可用时立即启动环境音；
- 允许 BGM 与低音量环境音同时存在；
- 声音控制必须可见、可理解；
- 不依赖 `fetch()` 本地 JSON 后才发现音频。

---

## 11. 新版 SpaceSpec

新版 `space-spec.json` 只记录直接影响页面的决策：

```json
{
  "book": {
    "title": "",
    "author": ""
  },
  "template": {
    "primary": "window|vinyl|instrument|route|symbols",
    "reason": ""
  },
  "readingCoordinate": {
    "world": "",
    "concern": "",
    "notice": "",
    "permission": ""
  },
  "bookDirection": {
    "visualMotif": "",
    "musicDirection": "",
    "textVoice": "",
    "motionCharacter": "",
    "uiLanguage": "",
    "avoid": []
  },
  "entryGuide": {
    "durationSec": 20,
    "steps": [
      {"id": "settle", "text": "", "visual": "", "audio": ""},
      {"id": "orient", "text": "", "visual": "", "audio": ""},
      {"id": "attune", "text": "", "visual": "", "audio": ""},
      {"id": "begin", "text": "", "visual": "", "audio": ""}
    ],
    "skipControl": true,
    "soundRequiredAfterStart": true
  },
  "visual": {
    "primaryPrompt": "",
    "imageRole": "",
    "safeArea": "",
    "windowVariations": []
  },
  "audio": {
    "bgmPrompt": "",
    "bgmFile": "./assets/audio/bgm.mp3",
    "ambienceFiles": [],
    "isInstrumental": true,
    "fallback": ""
  },
  "stages": [
    {
      "id": "",
      "label": "",
      "readingHint": "",
      "floatingTexts": [],
      "weather": "",
      "light": "",
      "ambience": "",
      "motion": "",
      "uiAccent": "",
      "image": ""
    }
  ],
  "weather": {
    "kind": "",
    "levels": ["off", "low", "medium"],
    "reducedMotionFallback": ""
  },
  "companion": {
    "preset": "frosted-glass|deck-controls|control-surface|wayfinder|symbol-tokens",
    "primaryCapabilities": ["entry-guide", "weather", "sound", "stages", "timer", "pomodoro"]
  },
  "firstScreen": {
    "aspectRatio": "16:9",
    "noVerticalCrowding": true,
    "noCriticalCrop": true
  },
  "fileProtocol": {
    "worksByDoubleClick": true,
    "noLocalFetchForJson": true,
    "relativeAssetsOnly": true
  }
}
```

必须删除旧字段和旧规则：

```text
signatureMechanic
topology
relations
anchors
completionCondition
companionTransformation
entry / exploration / companion 强制状态
```

---

## 12. 生成流程

```text
1. 获取书籍基础信息；
2. 生成无剧透读前坐标；
3. 读取 template-index，选择一个模板；
4. 仅读取选中模板；
5. 定义书籍身份与四个阅读阶段；
6. 生成 space-spec.json；
7. 生成一条 BGM Prompt，并异步启动 BGM；
8. 生成主图 Prompt；Window 生成母图后再按需图生图；
9. 选择必要环境音；
10. 加载共享能力和选中模板的陪伴组件骨架；
11. 生成 index.html、style.css、app.js；
12. 接入主图、天气、环境音与 BGM 状态；
13. 校验入静引导、声音、阶段切换、计时与番茄钟；
14. 校验 file:// 双击可用；
15. 校验 16:9 首屏；
16. 浏览器视觉扫描。
```

Agent 不得读取未选中的模板，也不得读取 Git 历史中的旧模板。

---

## 13. file:// 约束

核心能力必须支持用户直接双击 `index.html`。

要求：

- 不依赖本地 `fetch()` JSON；
- 关键配置内联到 `app.js` 或 HTML；
- 使用相对资源路径；
- 图片或 BGM 缺失时页面仍可运行；
- BGM 缺失时环境音仍能完成有声入静；
- 不要求开发服务器或在线 CDN。

---

## 14. 16:9 与响应式

桌面 16:9 是第一优先级。

至少验收：

```text
1366 × 768
1440 × 900
1920 × 1080
```

要求：

- 主要内容完整出现在首屏；
- 不发生上下拥挤；
- 不裁切关键主体；
- 不需要滚动才能理解页面；
- 陪伴组件不遮挡主体；
- 引导结束后不保留大面积遮罩。

移动端提供可用降级，但不是 V2 第一优先级。

---

## 15. 新版文档结构

```text
SKILL.md
V2版更新计划.md
README.md
references/
  template-index.md
  entry-guide.md
  companion-components.md
  space-spec-schema.md
  image-generation.md
  music-generation.md
  effects-recipes.md
  audio-manifest.json
  templates/
    window.md
    vinyl.md
    instrument.md
    route.md
    symbols.md
  tests/
scripts/
assets/
output/
```

删除所有旧模板和旧契约引用。

---

## 16. 实施顺序

### 里程碑 A：保存计划并清理旧版

- 提交本文；
- 删除旧签名机制、拓扑、三状态和旧模板残留；
- 删除旧校验规则；
- 确认 Git 历史仍可访问旧版。

### 里程碑 B：建立新版契约

- 重写 `SKILL.md`；
- 重写 SpaceSpec；
- 重写模板索引；
- 新增入静引导、陪伴组件、图片生成文档；
- 重写音乐与天气文档；
- 重写五个模板文档。

### 里程碑 C：预制共享基础

- 入静引导控制器；
- 声音与环境音兜底；
- 天气引擎；
- 阶段切换；
- 计时与番茄钟；
- file:// 兼容基础。

### 里程碑 D：模板陪伴组件

- Frosted Glass Panel；
- Deck Controls 与预制唱机；
- Control Surface；
- Wayfinder；
- Symbol Tokens。

### 里程碑 E：轻量运行校验与基础测试

每次生成任务只执行便宜、确定性的检查：

- 必要输出文件存在；
- JSON 可解析；
- JavaScript 无语法错误；
- HTML 正确加载 CSS 与 JS；
- HTML 引用的本地资源存在；
- 有浏览器工具时，打开页面并点击一次开始按钮，确认没有立即报错。

不要求生成 Agent 自评书籍贴合度、画面气质、模板个性或阶段表达。六项一级能力与五模板质量标准保留为产品验收要求，由共享运行时测试、真实样例实跑和人工抽查负责，而不是在每次生成后重复消耗上下文。

### 里程碑 F：逐模板实跑

为五个模板各生成至少一个测试页面，并完成浏览器视觉扫描。

---

## 17. 产品验收清单

以下清单用于 V2 开发阶段的真实样例评估和人工抽查，不作为每次生成任务的自动校验清单。

### 通用

- 活跃模板恰好为五个；
- 没有旧模板、拓扑、关系和三状态残留；
- 每次刷新回到入静首页；
- 用户点击开始后引导立即有声音；
- BGM 失败时环境音兜底；
- 全书只使用一首 BGM；
- 四个阅读阶段可手动切换；
- 天气可设为关闭 / 低 / 中；
- 阅读计时支持暂停、继续、重置；
- 番茄钟可用；
- 直接双击 `index.html` 可运行；
- 16:9 首屏无关键裁切和上下拥挤；
- `prefers-reduced-motion` 有降级；
- 页面贴合书籍，不是通用换皮。

### Window

- 一张母图和最多三张图生图变化；
- 四张图保持同一窗边空间；
- 前端没有重新绘制复杂窗框；
- 毛玻璃陪伴组件不遮挡主体。

### Vinyl

- 唱机主体清晰且可操作；
- 全书只有一张唱片与一首 BGM；
- 四阶段通过 Liner Notes、天气、灯光和文字表达；
- 不存在多唱片架和多音轨逻辑。

### Instrument

- 只有一个核心仪器；
- 控件可理解；
- Three.js 不是强制；
- 不像后台仪表盘。

### Route

- 路径清晰、稀疏；
- 四阶段可切换；
- 不像地图软件或寻路游戏。

### Symbols

- 3–5 个书籍专属象征物；
- 象征物提供观察视角；
- 没有占卜、抽牌、解谜和正确答案。

---

## 18. 最终定义

> VibeReading V2 为一本书生成一个高质量的 16:9 阅读陪伴空间。每次阅读都从一段有声音的短暂入静开始；随后，读者在主视觉、单首 BGM、必要环境音、克制天气、四个阅读阶段和模板专属陪伴组件中持续阅读。
